# Review build

Two things are in this repo at once:

1. **Fixes that are already applied** — corrections with no design judgement in
   them, listed under *Applied directly* below. These are live now.
2. **Two competing designs**, both coded, both switched by a `<body>` class:
   - **Wave one** (`p01`…`p18`) — individual improvements to the current design.
   - **Wave two** (`inst`…) — one coherent institutional design system.

Wave one is what is committed today. Wave two is the proposal.

## Seeing it

```
python -m http.server 8000
```

Then <http://127.0.0.1:8000>. A **Review** panel appears bottom-left with a
checkbox per item. It remembers your choices as you move between pages, and the
page links at the bottom of it walk you through the whole site.

The two waves are mutually exclusive — they answer the same questions
differently — so ticking anything in one group clears the other. **Reset** goes
back to what is committed.

`DEV_MODE` at the top of `scripts/proposals.js` is `false` — the deployed
setting. With it off the file returns immediately and renders nothing, so the
panel will not appear. **Set it back to `true` to review, and to `false` again
before deploying.**

## Choosing

The `<body>` class in the HTML is the single source of truth. Copy the string
from the panel onto `<body>` in `index.html`, `blog.html`, `article.html` and
the four policy pages to keep a set.

To adopt wave two:

```html
<body class="inst inst-hero inst-stats inst-process inst-team inst-services inst-results inst-faq inst-footer">
```

Note `inst-stats` has `[XX]` placeholders in it — leave it off until those
numbers exist.

---

## Applied directly

These needed no decision, so they are already in.

### Findable and shareable

| | |
|---|---|
| Canonical URLs | Every page. Article pages get theirs from `scripts/article.js`. |
| Share image | `images/og-image.png`, 1200×630, generated by `tools/build-brand-assets.py`. Open Graph and Twitter tags on every page. |
| Structured data | `EducationalOrganization` + `WebSite` on the home page, `FAQPage` for the answered questions, `Blog` on the index, `Article` + `BreadcrumbList` per post. Only verifiable claims are in it. |
| `sitemap.xml` | 12 URLs, generated by `tools/build-sitemap.py` from `data/blogInfo.js`. |
| `robots.txt` | Points at the sitemap. |
| Page titles | The home page was titled `TheUniMentor`; it is now `University Admissions Consulting \| TheUniMentor`. |
| Icons | A real set at 16/32/180/192/512 plus `site.webmanifest`. The old favicon was a 309&nbsp;KB full-resolution logo. |

### Weight

The site was shipping about 10&nbsp;MB of photography. `tools/build-images.py`
generates WebP and JPEG derivatives at 640/1024/1600/2560 into `images/opt/`;
originals are untouched and remain the `<picture>` fallback.

| | Before | After |
|---|---|---|
| Hero | 551 KB | 138 KB at 1600px |
| Each service photo | 1.0–1.8 MB | 40–144 KB |
| Merriweather | 9 weights | 5 |

Every `<img>` now carries `width`/`height` (no layout shift), `loading` and
`decoding`; the hero is preloaded at `fetchpriority="high"`.

Re-run `python tools/build-images.py` when a photo changes.

### Structure and access

- The home page had **no `<h1>`** — the hero statement is now one, and the
  section titles are `<h2>`, the card titles `<h3>`. The FAQ heading was a
  second `<h1>`.
- Desktop navigation is inside a `<nav>` landmark.
- A skip link on every page.
- Sections are `<section>` elements with `aria-labelledby`.
- University crests had empty `alt`; they now name the university
  (`uniName` in `data/tutorInfo.js`).

### Pages that did not exist

`privacy.html`, `terms.html`, `safeguarding.html`, `cookies.html`, `404.html`.
The footer's policy links previously pointed at `#home`.

**They are drafted templates, not legal advice, and they are not publishable as
they stand.** Each one carries a marked block naming what is missing. See below.

---

## What is still needed from you

Search for `[` before any deploy. With `DEV_MODE` on, the console lists every
placeholder left in the page on load.

### Blocking — these are wrong or unverifiable today

1. **Legal entity.** Registered name, jurisdiction, company number, registered
   office. Nothing in the footer or the policies can be finished without it, and
   it determines which privacy law applies.
2. **Safeguarding.** Are consultants background-checked, and is there a named
   person responsible? Most students here are under 18; this is the question
   parents look for. `safeguarding.html` cannot go live without an answer, and a
   claim that is not true is far worse than a missing page.
3. **Fees and cancellation terms.** `terms.html` cannot be completed without
   them, and UK/EU consumer law gives distance-purchase cancellation rights that
   have to be reflected.

### Non-blocking

4. Figures for the credibility band: years advising, students placed, subjects
   covered. Only rendered when `inst-stats` or `p06` is on; both are off.
5. Whether you work online, in person, or both — and where.
6. **`University Success Consulting™`.** The ™ symbol asserts a trademark. If
   the mark is not registered or in use as an unregistered mark, drop it.
7. **"countless students"** in the founder copy. A number, or a different word.
8. The testimonials are first name and city only. Attributed outcomes —
   subject, year, destination — are worth far more.

### Settled

The footer previously carried three claims invented in an earlier pass and
already live. All three are now resolved:

| Was | Now |
|---|---|
| *Management in United States* | Removed |
| *Established 2022 · London & Hong Kong* | **Established 2022** — year confirmed by you, locations dropped |
| *Registered legal entity name…* | *© 2026 TheUniMentor. All rights reserved.* |

**WeChat** is gone from all eight pages — the number and its footer row.
Contact is email and WhatsApp only.

With the committed `<body>` class, **no `[bracketed]` text renders anywhere on
the site**. Switch the institutional build on and two things surface, both of
which need a decision from you: the credibility figures, and the two FAQ answers
in items 2 and 5 above.

---

## Wave two — the institutional system

`styles/institutional.css`, scoped entirely to `body.inst`. With the class absent
the file changes nothing.

| Toggle | What it does |
|---|---|
| `inst` | The foundation, and the biggest change: flat ground instead of the tiled icon motif, no heading outlines, a serif/sans pairing, 2px corners instead of 40px pills, one 1200px container, consistent section rhythm. **Everything else assumes it.** |
| `inst-hero` | Scrim over the photograph instead of a floating white panel; eyebrow, headline, standfirst, two actions, and a line of proof. |
| `inst-stats` | Credibility band under the hero. **Placeholders.** |
| `inst-process` | New "How we work" section — four steps. Every claim in it is drawn from copy already on the site. |
| `inst-team` | Portrait frames, left-aligned names, crest on a footer rule. |
| `inst-services` | The carousel becomes a numbered three-up grid. Removes the auto-advance. |
| `inst-results` | Quote first, attribution small and pinned to the foot of the card. |
| `inst-faq` | Full-width rules; four new questions. **Two of them need answers.** |
| `inst-footer` | The wave-one footer, restyled to the system. |

### What it supersedes

Wave two absorbs the intent of `p02`, `p03`, `p04`, `p05`, `p10`, `p11`, `p12`,
`p13` and `p14`. If you adopt it, those become dead code and can be deleted
along with `styles/proposals.css`.

`p18` (WhatsApp as a labelled button) is **not** in wave two and is still worth
doing on its own — the floating bubble overlaps body copy on phones.

---

## Wave one — unchanged

| # | Proposal | Notes |
|---|---|---|
| 01 | Institutional footer | Live. Has placeholders |
| 02 | Plain ground | Brand risk — biggest visual change |
| 03 | Drop heading outlines | Turn on with 02 |
| 04 | Vertical rhythm | Subtle on its own |
| 05 | Container width | Live. Only visible at 1440px+ |
| 06 | Credibility strip | Has placeholders |
| 08 | 55/17 in type | Accessible, responsive |
| 09 | Safeguarding band | **Unverified claims** — do not enable |
| 10 | Testimonial hierarchy | |
| 11 | Nav dividers | Live. Desktop only |
| 12 | Hero legibility | |
| 13 | Services grid | Removes the auto-advance |
| 14 | Founder weight | Live |
| 18 | WhatsApp button | Brand risk |

---

## Known limitations

- **The header and footer are duplicated across eight HTML files.** That was
  already true of three; the policy pages made it worse. Either accept it and
  edit all eight together, or add a small build step. It is the main thing
  standing between this repo and being comfortable to maintain.
- **Google Fonts is a third-party request** on every page. Self-hosting the five
  Merriweather weights removes it, is faster, and removes the only item in
  `cookies.html` that needs explaining.
- **No enquiry form and no booking link.** Both CTAs open a dialog that asks
  people to send a WhatsApp message or an email. A form endpoint and a
  scheduling link would convert considerably better.
- **No analytics**, so none of the above can be measured. If you add any, the
  cookie policy needs a cookie table and, for UK/EU visitors, a consent banner
  that blocks the script until they agree.

## Files

```
styles/institutional.css   wave two
styles/proposals.css       wave one
styles/policy.css          the four policy documents
scripts/proposals.js       the review panel — DEV_MODE
scripts/siteChrome.js      header scroll state, in-page scrolling, placeholder guard
scripts/responsiveImage.js <picture> markup for script-rendered photos
tools/build-images.py      regenerates images/opt/
tools/build-brand-assets.py  regenerates the share image and icons
tools/build-sitemap.py     regenerates sitemap.xml
```

### To remove the scaffolding

Once a wave is chosen and folded into the main stylesheets, delete:
`scripts/proposals.js`, the losing stylesheet, the `REVIEW ONLY` link tags in
all eight HTML files, and the markup for whatever was not kept.
